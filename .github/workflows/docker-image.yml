name: Build and Publish image to Docker Hub

on:
  push:
    branches: [ "master" ]
  # pull_request:
  #   branches: [ "master" ]

jobs:
  publish_images:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set Docker image tags
      run: |
        # Use github.head_ref for PRs, github.ref_name for pushes
        if [ "${{ github.event_name }}" = "pull_request" ]; then
          BRANCH_NAME="${{ github.head_ref }}"
        else
          BRANCH_NAME="${{ github.ref_name }}"
        fi

        BRANCH_NAME_SAFE=$(echo "$BRANCH_NAME" | tr '/' '-')  

        echo "BRANCH_NAME=${BRANCH_NAME}" >> $GITHUB_ENV
        VERSION="v1.${{ github.run_number }}"
        echo "VERSION=${VERSION}" >> $GITHUB_ENV
        export VERSION

    - name: Build the Docker image
      run: |
        docker build \
          -t "${{ secrets.IMAGE_NAME }}:${{ env.BRANCH_NAME }}_${{ env.VERSION }}" .

    - name: Push images to Docker Hub
      run: |
        echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u ${{ secrets.DOCKER_HUB_USERNAME }} --password-stdin
        docker push "${{ secrets.IMAGE_NAME }}:${{ env.BRANCH_NAME }}_${{ env.VERSION }}"








# name: Build and Publish image to Docker Hub

# on:
#   push:
#     branches: [ "master" ]
#   pull_request:
#     branches: [ "master" ]

# jobs:
#   publish_images:
#     runs-on: ubuntu-latest

#     steps:
#     - name: checkout
#       uses: actions/checkout@v4
      
#     - name: Set version number
#       run: echo "VERSION=v1.${{ github.run_number }}" >> $GITHUB_ENV

#     - name: Build the Docker image
#       run: docker build -t "${{secrets.IMAGE_NAME}}":latest .
#     - name: push image to docker hub
#       run: |
#         echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u ${{ secrets.DOCKER_HUB_USERNAME }} --password-stdin
#         docker push "${{secrets.IMAGE_NAME}}":latest
